import React, { useState } from "react";
import { Modal, Button } from "react-bootstrap";

const CustomModal = ({ title, body }) => {
  return (
    <div>
      <>
        <Button variant="primary">Custom Width Modal</Button>
      </>
    </div>
  );
};

export default CustomModal;
